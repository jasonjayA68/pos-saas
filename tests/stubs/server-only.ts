// Stub for the `server-only` package, which throws when imported outside
// React Server Components. Tests run in plain Node so the real module
// errors at import. This empty stub is aliased in vitest.config.ts.
export {};
